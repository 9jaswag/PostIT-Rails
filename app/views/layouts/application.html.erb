<!DOCTYPE html>
<html>
  <head>
    <title>PostITRails</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="container">
      <nav class="navbar navbar-expand-lg navbar-light bg-transparent">
        <a class="navbar-brand" href="/">PostIT</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarToggle" aria-controls="navbarToggle" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarToggle">
          <div class="navbar-nav ml-md-auto d-md-flex">
            <% if logged_in? %>
              <%= link_to "Dashboard", groups_path, class: "nav-item nav-link active" %>
              <%= link_to "Log out", logout_path, method: :delete, class: "nav-item nav-link" %>
            <% end %>
            <% unless logged_in? %>
              <%= link_to "Get Started", users_path, class: "nav-link" %>
            <% end %>
            <a class="nav-item nav-link" href="/contact">Contact</a>
          </div>
        </div>
      </nav>
    </header>

    <div class="container yield-container">
      <%= yield %>
    </div>

    <div class="container">
      <footer class="mastfoot">
        <div class="inner">
          <p>&copy;2017 <a href="https://github.com/9jaswag/postit">PostIT</a>.</p>
        </div>
      </footer>
    </div>
  </body>
</html>
